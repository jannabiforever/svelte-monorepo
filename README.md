# svelte-monorepo
A template for a production app starter with monorepo structure

# Monorepo Structure

This template represents as follows:

- packages/*: Inner libraries for domain models, services, and utilities
- web: The main application using the inner libraries
